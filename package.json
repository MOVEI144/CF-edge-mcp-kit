{
  "name": "mcp-edge-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Reference architecture and reusable security primitives for Cloudflare-hosted remote MCP servers with passkeys, OAuth 2.1, zero-inbound-port device agents, and safe API connectors.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --experimental-strip-types --test packages/core/test/*.test.ts",
    "verify": "npm test && node scripts/verify-repo.mjs",
    "create:connector": "node scripts/create-connector.mjs"
  },
  "keywords": ["mcp","model-context-protocol","cloudflare-workers","oauth","passkeys","webauthn","durable-objects","chatgpt","zero-trust"]
}
